# Tarea2_IA
Tarea 2 de ia

dependencias:

pip install scikit-learn
pip install pandas

El archivo config.json tiene que estar en el mismo directorio que el archivo .py o .ipynb para que lo pueda leer.
